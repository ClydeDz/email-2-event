# Enforce a 14-day (1,209,600 second) cooldown on newly published packages.
# Prevents installing any npm package version published less than two weeks ago,
# reducing supply-chain risk from packages that could be quickly removed or compromised.
min-release-age=1209600
registry=https://registry.npmjs.org